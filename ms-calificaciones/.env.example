SERVICE_NAME=ms-calificaciones
ENV=development

REST_HOST=0.0.0.0
REST_PORT=3004

GRPC_HOST=0.0.0.0
GRPC_PORT=50054

# Orígenes permitidos (separados por coma). Usar * para desarrollo, URLs explícitas en producción.
CORS_ALLOWED_ORIGINS=*

# 'memory' o 'database'
STORAGE_MODE=database
DATABASE_URL=postgresql://user:password@localhost:5432/db_name